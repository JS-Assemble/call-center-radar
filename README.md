# call-center-radar

The project lives in [`callradar/`](callradar/) — see
[`callradar/README.md`](callradar/README.md) for setup from scratch (data
layout, running the pipeline, the dashboard, and the JSON API).